- name: Collecting Facts
  hosts: production
  gather_facts: yes

- name: Set up local development environment
  vars_files:
    - private/secrets.yml
  hosts: production
  roles:
    - earlystageconfigs
    - firewall
    - debrepos
    - users
    - services
    - secrets
